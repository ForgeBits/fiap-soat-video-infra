apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: fiapx
spec:
  type: LoadBalancer
  selector:
    app: api-service
  ports:
    - name: http
      protocol: TCP
      port: 81
      targetPort: 3001
