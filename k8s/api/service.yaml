apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: fiapx
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
